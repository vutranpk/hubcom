const fs = require("fs"); for(let i=1; i<=60; i++) { const progress = i/60; const r = 200 + Math.sin(progress * Math.PI) * 150; const cx = 640 + Math.cos(progress * Math.PI * 2) * 100; const cy = 360 + Math.sin(progress * Math.PI * 2) * 50; const svg = `<svg width="1280" height="720" xmlns="http://www.w3.org/2000/svg"><rect width="100%" height="100%" fill="#F7FAFF"/><circle cx="${cx}" cy="${cy}" r="${r}" fill="#2563EB" opacity="0.8"/><circle cx="${1280-cx}" cy="${720-cy}" r="${r*0.5}" fill="#06B6D4" opacity="0.6"/><text x="640" y="360" font-family="Arial" font-size="40" fill="#101828" text-anchor="middle">Test Frame ${i}</text></svg>`; fs.writeFileSync(`public/frames/frame_${i.toString().padStart(4, "0")}.svg`, svg); }
